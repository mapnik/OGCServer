Installing the OGCServer
------------------------

Dependencies
------------

    Mapnik >= 0.7.0 (python bindings)
    PIL
    lxml (--without-threads)


Installing
----------
    
Run the following command inside this directory (the directory that also contains the 'setup.py' file):

    $ sudo python setup.py install